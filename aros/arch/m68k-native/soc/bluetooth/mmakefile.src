include $(SRCDIR)/config/aros.cfg

USER_INCLUDES := -I$(SRCDIR)/arch/m68k-native/include -I$(SRCDIR)/arch/m68k-native/soc/include
USER_CFLAGS := -fno-common

%build_module mmake=kernel-btuart-emu68 \
  modname=btuart modtype=resource \
  files="btuart_init"
