include $(SRCDIR)/config/aros.cfg

USER_INCLUDES := -I$(SRCDIR)/arch/m68k-emu68/include
USER_CFLAGS := -fno-common

%build_module mmake=kernel-pl011bt-emu68 \
  modname=pl011bt modtype=resource \
  files="pl011bt_init"
