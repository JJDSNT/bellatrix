diff --git a/class/hub/usbh_hub.c b/class/hub/usbh_hub.c
index 5538de86..b9b6b5e2 100644
--- a/class/hub/usbh_hub.c
+++ b/class/hub/usbh_hub.c
@@ -27,6 +27,14 @@ extern void usbh_hubport_release(struct usbh_hubport *hport);
 
 static const char *speed_table[] = { "error-speed", "low-speed", "full-speed", "high-speed", "wireless-speed", "super-speed", "superplus-speed" };
 
+static void hub_port_status_decode(struct hub_port_status *port_status, const uint8_t *buf)
+{
+    port_status->wPortStatus = (uint16_t)buf[0] |
+                               ((uint16_t)buf[1] << 8);
+    port_status->wPortChange = (uint16_t)buf[2] |
+                               ((uint16_t)buf[3] << 8);
+}
+
 #if CONFIG_USBHOST_MAX_EXTHUBS > 0
 static struct usbh_hub g_hub_class[CONFIG_USBHOST_MAX_EXTHUBS];
 static uint32_t g_devinuse = 0;
@@ -118,7 +126,7 @@ static int _usbh_hub_get_portstatus(struct usbh_hub *hub, uint8_t port, struct h
     if (ret < 0) {
         return ret;
     }
-    memcpy(port_status, g_hub_buf[hub->bus->busid], 4);
+    hub_port_status_decode(port_status, g_hub_buf[hub->bus->busid]);
     return ret;
 }
 
@@ -219,7 +227,9 @@ static int parse_hub_ss_descriptor(struct usb_hub_ss_descriptor *desc, uint16_t
 static int usbh_hub_get_portstatus(struct usbh_hub *hub, uint8_t port, struct hub_port_status *port_status)
 {
     struct usb_setup_packet roothub_setup;
+    uint8_t roothub_buf[4];
     struct usb_setup_packet *setup;
+    int ret;
 
     if (hub->is_roothub) {
         setup = &roothub_setup;
@@ -228,7 +238,12 @@ static int usbh_hub_get_portstatus(struct usbh_hub *hub, uint8_t port, struct hu
         setup->wValue = 0;
         setup->wIndex = port;
         setup->wLength = 4;
-        return usbh_roothub_control(hub->bus, &roothub_setup, (uint8_t *)port_status);
+        ret = usbh_roothub_control(hub->bus, &roothub_setup, roothub_buf);
+        if (ret < 0) {
+            return ret;
+        }
+        hub_port_status_decode(port_status, roothub_buf);
+        return ret;
     } else {
         return _usbh_hub_get_portstatus(hub, port, port_status);
     }
@@ -496,9 +511,15 @@ static void usbh_hub_events(struct usbh_hub *hub)
     }
 
     flags = usb_osal_enter_critical_section();
-    memcpy(&portchange_index, hub->int_buffer, 2);
+    portchange_index = (uint16_t)hub->int_buffer[0] |
+                       ((uint16_t)hub->int_buffer[1] << 8);
     usb_osal_leave_critical_section(flags);
 
+    USB_LOG_INFO("Bellatrix USBH: portchange bitmap=0x%04x raw=%02x %02x\r\n",
+                 (unsigned int)portchange_index,
+                 (unsigned int)hub->int_buffer[0],
+                 (unsigned int)hub->int_buffer[1]);
+
     for (uint8_t port = 0; port < hub->nports; port++) {
         USB_LOG_DBG("Port change:0x%02x\r\n", portchange_index);
 
@@ -518,6 +539,11 @@ static void usbh_hub_events(struct usbh_hub *hub)
         portstatus = port_status.wPortStatus;
         portchange = port_status.wPortChange;
 
+        USB_LOG_INFO("Bellatrix USBH: port %u initial status=0x%04x change=0x%04x\r\n",
+                     (unsigned int)(port + 1),
+                     (unsigned int)portstatus,
+                     (unsigned int)portchange);
+
         USB_LOG_DBG("port %u, status:0x%03x, change:0x%02x\r\n", port + 1, portstatus, portchange);
 
         /* First, clear all change bits */
@@ -553,6 +579,13 @@ static void usbh_hub_events(struct usbh_hub *hub)
                 portstatus = port_status.wPortStatus;
                 portchange = port_status.wPortChange;
 
+                USB_LOG_INFO("Bellatrix USBH: port %u debounce status=0x%04x change=0x%04x stable=%u conn=0x%04x\r\n",
+                             (unsigned int)(port + 1),
+                             (unsigned int)portstatus,
+                             (unsigned int)portchange,
+                             (unsigned int)debouncestable,
+                             (unsigned int)connection);
+
                 USB_LOG_DBG("Port %u, status:0x%03x, change:0x%02x\r\n", port + 1, portstatus, portchange);
 
                 if (!(portchange & HUB_PORT_STATUS_C_CONNECTION) &&
@@ -600,6 +633,11 @@ static void usbh_hub_events(struct usbh_hub *hub)
                 portstatus = port_status.wPortStatus;
                 portchange = port_status.wPortChange;
 
+                USB_LOG_INFO("Bellatrix USBH: port %u post-reset status=0x%04x change=0x%04x\r\n",
+                             (unsigned int)(port + 1),
+                             (unsigned int)portstatus,
+                             (unsigned int)portchange);
+
                 USB_LOG_DBG("Port %u, status:0x%03x, change:0x%02x\r\n", port + 1, portstatus, portchange);
 
                 if (!(portstatus & HUB_PORT_STATUS_RESET) && (portstatus & HUB_PORT_STATUS_ENABLE)) {
@@ -709,11 +747,72 @@ static void usbh_hub_thread(CONFIG_USB_OSAL_THREAD_SET_ARGV)
     usb_osal_thread_delete(NULL);
 }
 
+static void usbh_hub_cleanup(struct usbh_bus *bus)
+{
+    struct usbh_hub *hub;
+    struct usbh_hubport *hport;
+
+    hub = &bus->hcd.roothub;
+    for (uint8_t port = 0; port < hub->nports; port++) {
+        hport = &hub->child[port];
+        usbh_hubport_release(hport);
+    }
+
+    if (bus->hub_mq) {
+        usb_osal_mq_delete(bus->hub_mq);
+        bus->hub_mq = NULL;
+    }
+    if (bus->hub_sem) {
+        usb_osal_sem_delete(bus->hub_sem);
+        bus->hub_sem = NULL;
+    }
+    if (bus->hub_started) {
+        usb_hc_deinit(bus);
+        bus->hub_started = 0;
+    }
+}
+
 void usbh_hub_thread_wakeup(struct usbh_hub *hub)
 {
+    USB_LOG_INFO("Bellatrix USBH: wake hub=%u root=%u intbuf=0x%02x\r\n",
+                 (unsigned int)hub->index,
+                 (unsigned int)hub->is_roothub,
+                 (unsigned int)hub->int_buffer[0]);
     usb_osal_mq_send(hub->bus->hub_mq, (uintptr_t)hub);
 }
 
+void usbh_hub_poll(struct usbh_bus *bus)
+{
+    struct usbh_hub *hub;
+    int ret;
+
+    if (!bus || !bus->hub_mq) {
+        return;
+    }
+
+    if (!bus->hub_started) {
+        bus->hub_started = 1;
+        bus->event_handler(bus->busid, USB_HUB_INDEX_ANY, USB_HUB_PORT_ANY, USB_INTERFACE_ANY, USBH_EVENT_INIT);
+        usb_hc_init(bus);
+    }
+
+    while (1) {
+        ret = usb_osal_mq_recv(bus->hub_mq, (uintptr_t *)&hub, 0);
+        if (ret < 0) {
+            break;
+        }
+        if (hub == NULL) {
+            usbh_hub_cleanup(bus);
+            break;
+        }
+        USB_LOG_INFO("Bellatrix USBH: poll hub event hub=%u root=%u intbuf=0x%02x\r\n",
+                     (unsigned int)hub->index,
+                     (unsigned int)hub->is_roothub,
+                     (unsigned int)hub->int_buffer[0]);
+        usbh_hub_events(hub);
+    }
+}
+
 int usbh_hub_initialize(struct usbh_bus *bus)
 {
     char thread_name[32] = { 0 };
@@ -728,6 +827,7 @@ int usbh_hub_initialize(struct usbh_bus *bus)
     hub->nports = CONFIG_USBHOST_MAX_RHPORTS;
     hub->int_buffer = bus->hcd.roothub_intbuf;
     hub->bus = bus;
+    bus->hub_started = 0;
 
     bus->hub_mq = usb_osal_mq_create(7);
     if (bus->hub_mq == NULL) {
@@ -756,11 +856,16 @@ int usbh_hub_deinitialize(struct usbh_bus *bus)
         return -USB_ERR_INVAL;
     }
 
+#ifdef CONFIG_USBHOST_BELLATRIX_POLLING
+    usb_osal_mq_send(bus->hub_mq, (uintptr_t)NULL);
+    usbh_hub_poll(bus);
+#else
     usb_osal_mq_send(bus->hub_mq, (uintptr_t)NULL);
     usb_osal_sem_take(bus->hub_sem, USB_OSAL_WAITING_FOREVER);
     usb_osal_sem_delete(bus->hub_sem);
     bus->hub_mq = NULL;
     bus->hub_sem = NULL;
+#endif
 
     bus->event_handler(bus->busid, USB_HUB_INDEX_ANY, USB_HUB_PORT_ANY, USB_INTERFACE_ANY, USBH_EVENT_DEINIT);
 
diff --git a/class/hub/usbh_hub.h b/class/hub/usbh_hub.h
index dcce6606..f8be7296 100644
--- a/class/hub/usbh_hub.h
+++ b/class/hub/usbh_hub.h
@@ -18,6 +18,7 @@ int usbh_hub_set_feature(struct usbh_hub *hub, uint8_t port, uint8_t feature);
 int usbh_hub_clear_feature(struct usbh_hub *hub, uint8_t port, uint8_t feature);
 
 void usbh_hub_thread_wakeup(struct usbh_hub *hub);
+void usbh_hub_poll(struct usbh_bus *bus);
 
 int usbh_hub_initialize(struct usbh_bus *bus);
 int usbh_hub_deinitialize(struct usbh_bus *bus);
diff --git a/core/usbh_core.c b/core/usbh_core.c
index cce9a46e..86727b8b 100644
--- a/core/usbh_core.c
+++ b/core/usbh_core.c
@@ -31,6 +31,11 @@ struct usbh_bus g_usbhost_bus[CONFIG_USBHOST_MAX_BUS];
 #define USB_DEV_ADDR_MARK_OFFSET 5
 #define USB_DEV_ADDR_MARK_MASK   0x1f
 
+static uint16_t usbh_read_le16(const uint8_t *p)
+{
+    return (uint16_t)p[0] | ((uint16_t)p[1] << 8);
+}
+
 static void dummy_event_handler(uint8_t busid, uint8_t hub_index, uint8_t hub_port, uint8_t intf, uint8_t event)
 {
     (void)busid;
@@ -132,6 +137,8 @@ static const struct usbh_class_driver *usbh_find_class_driver(uint8_t class, uin
 
 static int parse_device_descriptor(struct usbh_hubport *hport, struct usb_device_descriptor *desc, uint16_t length)
 {
+    const uint8_t *raw = (const uint8_t *)desc;
+
     if (desc->bLength != USB_SIZEOF_DEVICE_DESC) {
         USB_LOG_ERR("invalid device bLength 0x%02x\r\n", desc->bLength);
         return -USB_ERR_INVAL;
@@ -161,14 +168,14 @@ static int parse_device_descriptor(struct usbh_hubport *hport, struct usb_device
 #endif
         hport->device_desc.bLength = desc->bLength;
         hport->device_desc.bDescriptorType = desc->bDescriptorType;
-        hport->device_desc.bcdUSB = desc->bcdUSB;
+        hport->device_desc.bcdUSB = usbh_read_le16(raw + 2);
         hport->device_desc.bDeviceClass = desc->bDeviceClass;
         hport->device_desc.bDeviceSubClass = desc->bDeviceSubClass;
         hport->device_desc.bDeviceProtocol = desc->bDeviceProtocol;
         hport->device_desc.bMaxPacketSize0 = desc->bMaxPacketSize0;
-        hport->device_desc.idVendor = desc->idVendor;
-        hport->device_desc.idProduct = desc->idProduct;
-        hport->device_desc.bcdDevice = desc->bcdDevice;
+        hport->device_desc.idVendor = usbh_read_le16(raw + 8);
+        hport->device_desc.idProduct = usbh_read_le16(raw + 10);
+        hport->device_desc.bcdDevice = usbh_read_le16(raw + 12);
         hport->device_desc.iManufacturer = desc->iManufacturer;
         hport->device_desc.iProduct = desc->iProduct;
         hport->device_desc.iSerialNumber = desc->iSerialNumber;
@@ -211,7 +218,7 @@ static int parse_config_descriptor(struct usbh_hubport *hport, struct usb_config
 #endif
         hport->config.config_desc.bLength = desc->bLength;
         hport->config.config_desc.bDescriptorType = desc->bDescriptorType;
-        hport->config.config_desc.wTotalLength = desc->wTotalLength;
+        hport->config.config_desc.wTotalLength = usbh_read_le16((const uint8_t *)desc + 2);
         hport->config.config_desc.bNumInterfaces = desc->bNumInterfaces;
         hport->config.config_desc.bConfigurationValue = desc->bConfigurationValue;
         hport->config.config_desc.iConfiguration = desc->iConfiguration;
@@ -267,6 +274,8 @@ static int parse_config_descriptor(struct usbh_hubport *hport, struct usb_config
                 case USB_DESCRIPTOR_TYPE_ENDPOINT:
                     ep_desc = (struct usb_endpoint_descriptor *)p;
                     memcpy(&hport->config.intf[cur_iface].altsetting[cur_alt_setting].ep[cur_ep].ep_desc, ep_desc, 7);
+                    hport->config.intf[cur_iface].altsetting[cur_alt_setting].ep[cur_ep].ep_desc.wMaxPacketSize =
+                        usbh_read_le16(p + 4);
                     cur_ep++;
                     break;
 
@@ -299,14 +308,16 @@ static int usbh_get_default_mps(int speed)
     switch (speed) {
         case USB_SPEED_LOW: /* For low speed, we use 8 bytes */
             return 8;
-        case USB_SPEED_FULL: /* For full or high speed, we use 64 bytes */
+        case USB_SPEED_FULL: /* Start full-speed EP0 at 8 and reprogram after the first 8-byte descriptor read */
+            return 8;
         case USB_SPEED_HIGH:
+            /* High-speed EP0 is fixed at 64 bytes */
             return 64;
         case USB_SPEED_SUPER: /* For super speed , we must use 512 bytes */
         case USB_SPEED_SUPER_PLUS:
             return 512;
         default:
-            return 64;
+            return 8;
     }
 }
 
@@ -358,14 +369,14 @@ int usbh_enumerate(struct usbh_hubport *hport)
 
     /* Extract the correct max packetsize from the device descriptor */
     dev_desc = (struct usb_device_descriptor *)ep0_request_buffer[hport->bus->busid];
-    if (dev_desc->bcdUSB >= USB_3_0) {
+    if (hport->device_desc.bcdUSB >= USB_3_0) {
         ep_mps = 1 << dev_desc->bMaxPacketSize0;
     } else {
         ep_mps = dev_desc->bMaxPacketSize0;
     }
 
     USB_LOG_DBG("Device rev=%04x cls=%02x sub=%02x proto=%02x size=%d\r\n",
-                dev_desc->bcdUSB, dev_desc->bDeviceClass, dev_desc->bDeviceSubClass,
+                hport->device_desc.bcdUSB, dev_desc->bDeviceClass, dev_desc->bDeviceSubClass,
                 dev_desc->bDeviceProtocol, ep_mps);
 
     /* Reconfigure EP0 with the correct maximum packet size */
@@ -412,11 +423,11 @@ int usbh_enumerate(struct usbh_hubport *hport)
 
     parse_device_descriptor(hport, (struct usb_device_descriptor *)ep0_request_buffer[hport->bus->busid], USB_SIZEOF_DEVICE_DESC);
     USB_LOG_INFO("New device found,idVendor:%04x,idProduct:%04x,bcdDevice:%04x\r\n",
-                 ((struct usb_device_descriptor *)ep0_request_buffer[hport->bus->busid])->idVendor,
-                 ((struct usb_device_descriptor *)ep0_request_buffer[hport->bus->busid])->idProduct,
-                 ((struct usb_device_descriptor *)ep0_request_buffer[hport->bus->busid])->bcdDevice);
+                 hport->device_desc.idVendor,
+                 hport->device_desc.idProduct,
+                 hport->device_desc.bcdDevice);
 
-    USB_LOG_INFO("The device has %d bNumConfigurations\r\n", ((struct usb_device_descriptor *)ep0_request_buffer[hport->bus->busid])->bNumConfigurations);
+    USB_LOG_INFO("The device has %d bNumConfigurations\r\n", hport->device_desc.bNumConfigurations);
 
     config_index = usbh_get_hport_active_config_index(hport);
     USB_LOG_DBG("The device selects config %d\r\n", config_index);
@@ -441,7 +452,7 @@ int usbh_enumerate(struct usbh_hubport *hport)
     }
 
     /* Read the full size of the configuration data */
-    uint16_t wTotalLength = ((struct usb_configuration_descriptor *)ep0_request_buffer[hport->bus->busid])->wTotalLength;
+    uint16_t wTotalLength = usbh_read_le16(&ep0_request_buffer[hport->bus->busid][2]);
 
     if (wTotalLength > CONFIG_USBHOST_REQUEST_BUFFER_LEN) {
         ret = -USB_ERR_NOMEM;
@@ -467,7 +478,7 @@ int usbh_enumerate(struct usbh_hubport *hport)
         goto errout;
     }
 
-    USB_LOG_INFO("The device has %d interfaces\r\n", ((struct usb_configuration_descriptor *)ep0_request_buffer[hport->bus->busid])->bNumInterfaces);
+    USB_LOG_INFO("The device has %d interfaces\r\n", hport->config.config_desc.bNumInterfaces);
     hport->raw_config_desc = usb_osal_malloc(wTotalLength + 1);
     if (hport->raw_config_desc == NULL) {
         ret = -USB_ERR_NOMEM;
@@ -475,7 +486,7 @@ int usbh_enumerate(struct usbh_hubport *hport)
         goto errout;
     }
 
-    config_value = ((struct usb_configuration_descriptor *)ep0_request_buffer[hport->bus->busid])->bConfigurationValue;
+    config_value = hport->config.config_desc.bConfigurationValue;
     memcpy(hport->raw_config_desc, ep0_request_buffer[hport->bus->busid], wTotalLength);
     hport->raw_config_desc[wTotalLength] = '\0';
 
@@ -693,6 +704,16 @@ int usbh_control_transfer(struct usbh_hubport *hport, struct usb_setup_packet *s
     usbh_print_setup(setup);
 
 resubmit:
+    USB_LOG_INFO("Bellatrix USBH: ctrl xfer addr=%u hub=%u port=%u bmReq=0x%02x bReq=0x%02x wValue=0x%04x wIndex=0x%04x wLength=%u retry=%u\r\n",
+                 (unsigned int)hport->dev_addr,
+                 (unsigned int)hport->parent->index,
+                 (unsigned int)hport->port,
+                 (unsigned int)setup->bmRequestType,
+                 (unsigned int)setup->bRequest,
+                 (unsigned int)setup->wValue,
+                 (unsigned int)setup->wIndex,
+                 (unsigned int)setup->wLength,
+                 (unsigned int)retry);
     usbh_control_urb_fill(urb, hport, setup, buffer, setup->wLength, CONFIG_USBHOST_CONTROL_TRANSFER_TIMEOUT, NULL, NULL);
     ret = usbh_submit_urb(urb);
     if (ret == 0) {
@@ -707,6 +728,21 @@ resubmit:
         }
     }
 
+    if (ret < 0) {
+        USB_LOG_ERR("Bellatrix USBH: ctrl xfer failed ret=%d addr=%u bReq=0x%02x wValue=0x%04x wLength=%u\r\n",
+                    ret,
+                    (unsigned int)hport->dev_addr,
+                    (unsigned int)setup->bRequest,
+                    (unsigned int)setup->wValue,
+                    (unsigned int)setup->wLength);
+    } else {
+        USB_LOG_INFO("Bellatrix USBH: ctrl xfer complete actual=%d addr=%u bReq=0x%02x wValue=0x%04x\r\n",
+                     ret,
+                     (unsigned int)hport->dev_addr,
+                     (unsigned int)setup->bRequest,
+                     (unsigned int)setup->wValue);
+    }
+
     usb_osal_mutex_give(hport->mutex);
     return ret;
 }
diff --git a/core/usbh_core.h b/core/usbh_core.h
index abd193df..0d1b60f9 100644
--- a/core/usbh_core.h
+++ b/core/usbh_core.h
@@ -199,6 +199,7 @@ struct usbh_hcd {
 struct usbh_bus {
     usb_slist_t list;
     uint8_t busid;
+    uint8_t hub_started;
     struct usbh_hcd hcd;
     struct usbh_devaddr_map devgen;
     usb_osal_thread_t hub_thread;
diff --git a/port/dwc2/usb_dwc2_param.h b/port/dwc2/usb_dwc2_param.h
index 74e436c2..908a2954 100644
--- a/port/dwc2/usb_dwc2_param.h
+++ b/port/dwc2/usb_dwc2_param.h
@@ -6,13 +6,15 @@
 #ifndef __USB_DWC2_PARAM_H__
 #define __USB_DWC2_PARAM_H__
 
+#include "support.h"
+
 /* Maximum number of Endpoints/HostChannels */
 #define MAX_EPS_CHANNELS 16
 
 #define HSOTG_REG(x) (x)
 
 #define dwc2_readl(addr) \
-    (*(volatile uint32_t *)(addr))
+    rd32le((uintptr_t)(addr))
 
 #define GUID_OFFSET HSOTG_REG(0x003C)
 
@@ -364,4 +366,4 @@ static inline void dwc2_get_hwparams(uint32_t reg_base, struct dwc2_hw_params *h
 void dwc2_get_user_params(uint32_t reg_base, struct dwc2_user_params *params);
 void dwc2_get_user_fifo_config(uint32_t reg_base, struct usb_dwc2_user_fifo_config *config);
 
-#endif
\ No newline at end of file
+#endif
diff --git a/port/dwc2/usb_hc_dwc2.c b/port/dwc2/usb_hc_dwc2.c
index 946dfd46..03362224 100644
--- a/port/dwc2/usb_hc_dwc2.c
+++ b/port/dwc2/usb_hc_dwc2.c
@@ -7,12 +7,38 @@
 #include "usbh_hub.h"
 #include "usb_dwc2_reg.h"
 #include "usb_dwc2_param.h"
+#include "mmu.h"
+#include <stddef.h>
 
-#define USB_OTG_GLB     ((DWC2_GlobalTypeDef *)(bus->hcd.reg_base))
-#define USB_OTG_PCGCCTL *(__IO uint32_t *)((uint32_t)bus->hcd.reg_base + USB_OTG_PCGCCTL_BASE)
-#define USB_OTG_HPRT    *(__IO uint32_t *)((uint32_t)bus->hcd.reg_base + USB_OTG_HOST_PORT_BASE)
-#define USB_OTG_HOST    ((DWC2_HostTypeDef *)(bus->hcd.reg_base + USB_OTG_HOST_BASE))
-#define USB_OTG_HC(i)   ((DWC2_HostChannelTypeDef *)(bus->hcd.reg_base + USB_OTG_HOST_CHANNEL_BASE + ((i)*USB_OTG_HOST_CHANNEL_SIZE)))
+#define USB_OTG_GLB     ((DWC2_GlobalTypeDef *)(uintptr_t)(bus->hcd.reg_base))
+#define USB_OTG_PCGCCTL (*(volatile uint32_t *)(uintptr_t)(bus->hcd.reg_base + USB_OTG_PCGCCTL_BASE))
+#define USB_OTG_HPRT    (*(volatile uint32_t *)(uintptr_t)(bus->hcd.reg_base + USB_OTG_HOST_PORT_BASE))
+#define USB_OTG_HOST    ((DWC2_HostTypeDef *)(uintptr_t)(bus->hcd.reg_base + USB_OTG_HOST_BASE))
+#define USB_OTG_HC(i)   ((DWC2_HostChannelTypeDef *)(uintptr_t)(bus->hcd.reg_base + USB_OTG_HOST_CHANNEL_BASE + ((i)*USB_OTG_HOST_CHANNEL_SIZE)))
+
+#define DWC2_GLB_OFF(field)   ((uint32_t)offsetof(DWC2_GlobalTypeDef, field))
+#define DWC2_HOST_OFF(field)  ((uint32_t)(USB_OTG_HOST_BASE + offsetof(DWC2_HostTypeDef, field)))
+#define DWC2_HC_OFF(i, field) ((uint32_t)(USB_OTG_HOST_CHANNEL_BASE + ((i) * USB_OTG_HOST_CHANNEL_SIZE) + offsetof(DWC2_HostChannelTypeDef, field)))
+
+static inline uint32_t dwc2_rd32(struct usbh_bus *bus, uint32_t off)
+{
+    return rd32le((uintptr_t)(bus->hcd.reg_base + off));
+}
+
+static inline void dwc2_wr32(struct usbh_bus *bus, uint32_t off, uint32_t val)
+{
+    wr32le((uintptr_t)(bus->hcd.reg_base + off), val);
+}
+
+static inline uint32_t dwc2_hc_rd32(struct usbh_bus *bus, uint8_t ch_num, uint32_t field_off)
+{
+    return dwc2_rd32(bus, DWC2_HC_OFF(ch_num, HCCHAR) - offsetof(DWC2_HostChannelTypeDef, HCCHAR) + field_off);
+}
+
+static inline void dwc2_hc_wr32(struct usbh_bus *bus, uint8_t ch_num, uint32_t field_off, uint32_t val)
+{
+    dwc2_wr32(bus, DWC2_HC_OFF(ch_num, HCCHAR) - offsetof(DWC2_HostChannelTypeDef, HCCHAR) + field_off, val);
+}
 
 struct dwc2_chan {
     uint8_t ep0_state;
@@ -25,6 +51,8 @@ struct dwc2_chan {
     uint8_t hub_addr;
     uint8_t hub_port;
     uint16_t ssplit_frame;
+    uint16_t last_start_frame;
+    uint8_t sof_kicks;
     usb_osal_sem_t waitsem;
     struct usbh_urb *urb;
     uint32_t iso_frame_idx;
@@ -45,6 +73,42 @@ struct dwc2_hcd {
 #define DWC2_EP0_STATE_INSTATUS  3
 #define DWC2_EP0_STATE_OUTSTATUS 4
 
+static inline uint16_t dwc2_get_full_frame_num(struct usbh_bus *bus);
+static void dwc2_inchan_irq_handler(struct usbh_bus *bus, uint8_t ch_num);
+static void dwc2_outchan_irq_handler(struct usbh_bus *bus, uint8_t ch_num);
+
+static bool dwc2_service_pending_channels(struct usbh_bus *bus, bool log_fallback)
+{
+    uint32_t chan_int;
+    bool handled = false;
+
+    chan_int = (dwc2_rd32(bus, DWC2_HOST_OFF(HAINT)) &
+                dwc2_rd32(bus, DWC2_HOST_OFF(HAINTMSK))) & 0xFFFFU;
+
+    for (uint8_t i = 0U; i < g_dwc2_hcd[bus->hcd.hcd_id].hw_params.host_channels; i++) {
+        if ((chan_int & (1UL << (i & 0xFU))) == 0U) {
+            continue;
+        }
+
+        handled = true;
+        if (log_fallback) {
+            USB_LOG_INFO("Bellatrix DWC2: fallback ch=%u hcint=0x%08x hcchar=0x%08x haint=0x%08x\r\n",
+                         (unsigned int)i,
+                         (unsigned int)dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCINT)),
+                         (unsigned int)dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCCHAR)),
+                         (unsigned int)dwc2_rd32(bus, DWC2_HOST_OFF(HAINT)));
+        }
+
+        if ((dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCCHAR)) & USB_OTG_HCCHAR_EPDIR) == USB_OTG_HCCHAR_EPDIR) {
+            dwc2_inchan_irq_handler(bus, i);
+        } else {
+            dwc2_outchan_irq_handler(bus, i);
+        }
+    }
+
+    return handled;
+}
+
 static inline int dwc2_reset(struct usbh_bus *bus)
 {
     volatile uint32_t count = 0U;
@@ -54,11 +118,12 @@ static inline int dwc2_reset(struct usbh_bus *bus)
         if (++count > 200000U) {
             return -1;
         }
-    } while ((USB_OTG_GLB->GRSTCTL & USB_OTG_GRSTCTL_AHBIDL) == 0U);
+    } while ((dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & USB_OTG_GRSTCTL_AHBIDL) == 0U);
 
     /* Core Soft Reset */
     count = 0U;
-    USB_OTG_GLB->GRSTCTL |= USB_OTG_GRSTCTL_CSRST;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GRSTCTL),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) | USB_OTG_GRSTCTL_CSRST);
 
     if (g_dwc2_hcd[bus->hcd.hcd_id].hw_params.snpsid < 0x4F54420AU) {
         do {
@@ -66,17 +131,19 @@ static inline int dwc2_reset(struct usbh_bus *bus)
                 USB_LOG_ERR("DWC2 reset timeout\r\n");
                 return -1;
             }
-        } while ((USB_OTG_GLB->GRSTCTL & USB_OTG_GRSTCTL_CSRST) == USB_OTG_GRSTCTL_CSRST);
+        } while ((dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & USB_OTG_GRSTCTL_CSRST) == USB_OTG_GRSTCTL_CSRST);
     } else {
         do {
             if (++count > 200000U) {
                 USB_LOG_ERR("DWC2 reset timeout\r\n");
                 return -1;
             }
-        } while ((USB_OTG_GLB->GRSTCTL & USB_OTG_GRSTCTL_CSRSTDONE) != USB_OTG_GRSTCTL_CSRSTDONE);
+        } while ((dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & USB_OTG_GRSTCTL_CSRSTDONE) != USB_OTG_GRSTCTL_CSRSTDONE);
 
-        USB_OTG_GLB->GRSTCTL &= ~USB_OTG_GRSTCTL_CSRST;
-        USB_OTG_GLB->GRSTCTL |= USB_OTG_GRSTCTL_CSRSTDONE;
+        dwc2_wr32(bus, DWC2_GLB_OFF(GRSTCTL),
+                  dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & ~USB_OTG_GRSTCTL_CSRST);
+        dwc2_wr32(bus, DWC2_GLB_OFF(GRSTCTL),
+                  dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) | USB_OTG_GRSTCTL_CSRSTDONE);
     }
 
     return 0;
@@ -89,9 +156,10 @@ static inline int dwc2_core_init(struct usbh_bus *bus)
 
     if (g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_type == DWC2_PHY_TYPE_PARAM_FS) {
         /* Select FS Embedded PHY */
-        USB_OTG_GLB->GUSBCFG |= USB_OTG_GUSBCFG_PHYSEL;
+        dwc2_wr32(bus, DWC2_GLB_OFF(GUSBCFG),
+                  dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG)) | USB_OTG_GUSBCFG_PHYSEL);
     } else {
-        regval = USB_OTG_GLB->GUSBCFG;
+        regval = dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG));
         regval &= ~USB_OTG_GUSBCFG_PHYSEL;
         /* disable external vbus source */
         regval &= ~(USB_OTG_GUSBCFG_ULPIEVBUSD | USB_OTG_GUSBCFG_ULPIEVBUSI);
@@ -117,7 +185,7 @@ static inline int dwc2_core_init(struct usbh_bus *bus)
             default:
                 break;
         }
-        USB_OTG_GLB->GUSBCFG = regval;
+        dwc2_wr32(bus, DWC2_GLB_OFF(GUSBCFG), regval);
     }
 
     /* Reset after a PHY select */
@@ -127,16 +195,19 @@ static inline int dwc2_core_init(struct usbh_bus *bus)
 
 static inline void dwc2_set_mode(struct usbh_bus *bus, uint8_t mode)
 {
-    USB_OTG_GLB->GUSBCFG &= ~(USB_OTG_GUSBCFG_FHMOD | USB_OTG_GUSBCFG_FDMOD);
+    dwc2_wr32(bus, DWC2_GLB_OFF(GUSBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG)) & ~(USB_OTG_GUSBCFG_FHMOD | USB_OTG_GUSBCFG_FDMOD));
 
     if (mode == USB_OTG_MODE_HOST) {
-        USB_OTG_GLB->GUSBCFG |= USB_OTG_GUSBCFG_FHMOD;
+        dwc2_wr32(bus, DWC2_GLB_OFF(GUSBCFG),
+                  dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG)) | USB_OTG_GUSBCFG_FHMOD);
     } else if (mode == USB_OTG_MODE_DEVICE) {
-        USB_OTG_GLB->GUSBCFG |= USB_OTG_GUSBCFG_FDMOD;
+        dwc2_wr32(bus, DWC2_GLB_OFF(GUSBCFG),
+                  dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG)) | USB_OTG_GUSBCFG_FDMOD);
     }
 
     while (1) {
-        if ((USB_OTG_GLB->GINTSTS & 0x1U) == USB_OTG_MODE_HOST) {
+        if ((dwc2_rd32(bus, DWC2_GLB_OFF(GINTSTS)) & 0x1U) == USB_OTG_MODE_HOST) {
             break;
         }
         usb_osal_msleep(10);
@@ -152,16 +223,16 @@ static inline int dwc2_flush_rxfifo(struct usbh_bus *bus)
         if (++count > 200000U) {
             return -1;
         }
-    } while ((USB_OTG_GLB->GRSTCTL & USB_OTG_GRSTCTL_AHBIDL) == 0U);
+    } while ((dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & USB_OTG_GRSTCTL_AHBIDL) == 0U);
 
     count = 0;
-    USB_OTG_GLB->GRSTCTL = USB_OTG_GRSTCTL_RXFFLSH;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GRSTCTL), USB_OTG_GRSTCTL_RXFFLSH);
 
     do {
         if (++count > 200000U) {
             return -1;
         }
-    } while ((USB_OTG_GLB->GRSTCTL & USB_OTG_GRSTCTL_RXFFLSH) == USB_OTG_GRSTCTL_RXFFLSH);
+    } while ((dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & USB_OTG_GRSTCTL_RXFFLSH) == USB_OTG_GRSTCTL_RXFFLSH);
 
     return 0;
 }
@@ -175,16 +246,16 @@ static inline int dwc2_flush_txfifo(struct usbh_bus *bus, uint32_t num)
         if (++count > 200000U) {
             return -1;
         }
-    } while ((USB_OTG_GLB->GRSTCTL & USB_OTG_GRSTCTL_AHBIDL) == 0U);
+    } while ((dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & USB_OTG_GRSTCTL_AHBIDL) == 0U);
 
     count = 0;
-    USB_OTG_GLB->GRSTCTL = (USB_OTG_GRSTCTL_TXFFLSH | (num << 6));
+    dwc2_wr32(bus, DWC2_GLB_OFF(GRSTCTL), (USB_OTG_GRSTCTL_TXFFLSH | (num << 6)));
 
     do {
         if (++count > 200000U) {
             return -1;
         }
-    } while ((USB_OTG_GLB->GRSTCTL & USB_OTG_GRSTCTL_TXFFLSH) == USB_OTG_GRSTCTL_TXFFLSH);
+    } while ((dwc2_rd32(bus, DWC2_GLB_OFF(GRSTCTL)) & USB_OTG_GRSTCTL_TXFFLSH) == USB_OTG_GRSTCTL_TXFFLSH);
 
     return 0;
 }
@@ -193,16 +264,16 @@ static inline void dwc2_drivebus(struct usbh_bus *bus, uint8_t state)
 {
     __IO uint32_t hprt0 = 0U;
 
-    hprt0 = USB_OTG_HPRT;
+    hprt0 = dwc2_rd32(bus, DWC2_HOST_OFF(HPRT));
 
     hprt0 &= ~(USB_OTG_HPRT_PENA | USB_OTG_HPRT_PCDET |
                USB_OTG_HPRT_PENCHNG | USB_OTG_HPRT_POCCHNG);
 
     if (((hprt0 & USB_OTG_HPRT_PPWR) == 0U) && (state == 1U)) {
-        USB_OTG_HPRT = (USB_OTG_HPRT_PPWR | hprt0);
+        dwc2_wr32(bus, DWC2_HOST_OFF(HPRT), (USB_OTG_HPRT_PPWR | hprt0));
     }
     if (((hprt0 & USB_OTG_HPRT_PPWR) == USB_OTG_HPRT_PPWR) && (state == 0U)) {
-        USB_OTG_HPRT = ((~USB_OTG_HPRT_PPWR) & hprt0);
+        dwc2_wr32(bus, DWC2_HOST_OFF(HPRT), ((~USB_OTG_HPRT_PPWR) & hprt0));
     }
 }
 
@@ -210,8 +281,9 @@ static inline uint8_t usbh_get_port_speed(struct usbh_bus *bus, const uint8_t po
 {
     __IO uint32_t hprt0 = 0U;
     uint8_t speed;
+    (void)port;
 
-    hprt0 = USB_OTG_HPRT;
+    hprt0 = dwc2_rd32(bus, DWC2_HOST_OFF(HPRT));
 
     speed = (hprt0 & USB_OTG_HPRT_PSPD) >> 17;
 
@@ -257,7 +329,7 @@ static inline void dwc2_chan_char_init(struct usbh_bus *bus,
         regval |= USB_OTG_HCCHAR_ODDFRM;
     }
 
-    USB_OTG_HC((uint32_t)ch_num)->HCCHAR = regval;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR), regval);
 }
 
 static inline void dwc2_chan_splt_init(struct usbh_bus *bus, uint8_t ch_num)
@@ -278,9 +350,9 @@ static inline void dwc2_chan_splt_init(struct usbh_bus *bus, uint8_t ch_num)
             hcsplt &= ~USB_OTG_HCSPLT_COMPLSPLT;
         }
 
-        USB_OTG_HC((uint32_t)ch_num)->HCSPLT = hcsplt;
+        dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCSPLT), hcsplt);
     } else {
-        USB_OTG_HC((uint32_t)ch_num)->HCSPLT = 0U;
+        dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCSPLT), 0U);
     }
 }
 
@@ -293,13 +365,14 @@ static void dwc2_chan_init(struct usbh_bus *bus,
                            uint8_t ep_mult,
                            uint8_t speed)
 {
-    USB_OTG_HC((uint32_t)ch_num)->HCINTMSK = 0U;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINTMSK), 0U);
 
     /* Clear old interrupt conditions for this host channel. */
-    USB_OTG_HC((uint32_t)ch_num)->HCINT = 0xFFFFFFFFU;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINT), 0xFFFFFFFFU);
 
     /* Enable the top level host channel interrupt. */
-    USB_OTG_HOST->HAINTMSK |= 1UL << (ch_num & 0xFU);
+    dwc2_wr32(bus, DWC2_HOST_OFF(HAINTMSK),
+              dwc2_rd32(bus, DWC2_HOST_OFF(HAINTMSK)) | (1UL << (ch_num & 0xFU)));
 
     dwc2_chan_char_init(bus, ch_num, dev_addr, ep_addr, ep_type, ep_mps, ep_mult, speed);
     dwc2_chan_splt_init(bus, ch_num);
@@ -308,87 +381,187 @@ static void dwc2_chan_init(struct usbh_bus *bus,
 static inline void dwc2_chan_transfer(struct usbh_bus *bus, uint8_t ch_num, uint8_t ep_addr, uint8_t *buf, uint32_t size, uint16_t num_packets, uint8_t pid)
 {
     __IO uint32_t tmpreg;
+    uint32_t dma_phys;
+    uint32_t dma_addr;
+    uint32_t hcintmsk;
     uint8_t is_oddframe;
     size_t flags;
+    struct dwc2_chan *chan;
+
+    (void)ep_addr;
+
+    chan = &g_dwc2_hcd[bus->hcd.hcd_id].chan_pool[ch_num];
 
     /* Initialize the HCTSIZn register */
-    USB_OTG_HC(ch_num)->HCTSIZ = (size & USB_OTG_HCTSIZ_XFRSIZ) |
-                                 (((uint32_t)num_packets << 19) & USB_OTG_HCTSIZ_PKTCNT) |
-                                 (((uint32_t)pid << 29) & USB_OTG_HCTSIZ_DPID);
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCTSIZ),
+                 (size & USB_OTG_HCTSIZ_XFRSIZ) |
+                 (((uint32_t)num_packets << 19) & USB_OTG_HCTSIZ_PKTCNT) |
+                 (((uint32_t)pid << 29) & USB_OTG_HCTSIZ_DPID));
 
     /* xfer_buff MUST be 32-bits aligned */
-    USB_OTG_HC(ch_num)->HCDMA = (uint32_t)buf;
+    dma_phys = buf ? (uint32_t)mmu_virt2phys((uintptr_t)buf) : 0U;
+    dma_addr = dma_phys;
+    if (dma_addr != 0U &&
+        g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_type != DWC2_PHY_TYPE_PARAM_FS) {
+        dma_addr |= 0xC0000000U;
+    }
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCDMA), dma_addr);
 
     flags = usb_osal_enter_critical_section();
 
     /* Enable channel interrupts required for this transfer. */
-    USB_OTG_HC((uint32_t)ch_num)->HCINTMSK = USB_OTG_HCINTMSK_CHHM;
-
-    is_oddframe = (((uint32_t)USB_OTG_HOST->HFNUM & 0x01U) != 0U) ? 0U : 1U;
-    USB_OTG_HC(ch_num)->HCCHAR &= ~USB_OTG_HCCHAR_ODDFRM;
-    USB_OTG_HC(ch_num)->HCCHAR |= (uint32_t)is_oddframe << 29;
+    hcintmsk = USB_OTG_HCINTMSK_XFRCM | USB_OTG_HCINTMSK_CHHM | USB_OTG_HCINTMSK_STALLM |
+               USB_OTG_HCINTMSK_NAKM | USB_OTG_HCINTMSK_ACKM | USB_OTG_HCINTMSK_NYET |
+               USB_OTG_HCINTMSK_TXERRM | USB_OTG_HCINTMSK_BBERRM |
+               USB_OTG_HCINTMSK_FRMORM | USB_OTG_HCINTMSK_DTERRM | USB_OTG_HCINTMSK_AHBERR;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINTMSK), hcintmsk);
+
+    is_oddframe = (((uint32_t)dwc2_rd32(bus, DWC2_HOST_OFF(HFNUM)) & 0x01U) != 0U) ? 1U : 0U;
+    tmpreg = dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR));
+    tmpreg &= ~USB_OTG_HCCHAR_ODDFRM;
+    tmpreg |= (uint32_t)is_oddframe << 29;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR), tmpreg);
 
     /* Set host channel enable */
-    tmpreg = USB_OTG_HC(ch_num)->HCCHAR;
+    tmpreg = dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR));
     tmpreg &= ~USB_OTG_HCCHAR_CHDIS;
     tmpreg |= USB_OTG_HCCHAR_CHENA;
-    USB_OTG_HC(ch_num)->HCCHAR = tmpreg;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR), tmpreg);
+
+    if (chan->urb && USB_GET_ENDPOINT_TYPE(chan->urb->ep->bmAttributes) == USB_ENDPOINT_TYPE_CONTROL) {
+        USB_LOG_INFO("Bellatrix DWC2: arm ch=%u ep=0x%02x state=%u hcintmsk=0x%08x hctsiz=0x%08x hcchar=0x%08x hcdma=0x%08x pid=%u size=%u pkts=%u\r\n",
+                     (unsigned int)ch_num,
+                     (unsigned int)ep_addr,
+                     (unsigned int)chan->ep0_state,
+                     (unsigned int)hcintmsk,
+                     (unsigned int)dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCTSIZ)),
+                     (unsigned int)dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR)),
+                     (unsigned int)dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCDMA)),
+                     (unsigned int)pid,
+                     (unsigned int)size,
+                     (unsigned int)num_packets);
+        USB_LOG_INFO("Bellatrix DWC2: dma buf=%p phys=0x%08x\r\n", buf, (unsigned int)dma_phys);
+        USB_LOG_INFO("Bellatrix DWC2: dma hcdma=0x%08x\r\n", (unsigned int)dma_addr);
+    }
+
+    chan->last_start_frame = dwc2_get_full_frame_num(bus);
+    chan->sof_kicks = 0;
 
     usb_osal_leave_critical_section(flags);
 }
 
+static void dwc2_sof_kick_pending_channels(struct usbh_bus *bus)
+{
+    uint16_t frame;
+
+    if (g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_type == DWC2_PHY_TYPE_PARAM_FS) {
+        return;
+    }
+
+    frame = dwc2_get_full_frame_num(bus);
+
+    for (uint8_t i = 0U; i < g_dwc2_hcd[bus->hcd.hcd_id].hw_params.host_channels; i++) {
+        struct dwc2_chan *chan;
+        struct usbh_urb *urb;
+        uint32_t hcchar;
+        uint32_t hcint;
+
+        chan = &g_dwc2_hcd[bus->hcd.hcd_id].chan_pool[i];
+        urb = chan->urb;
+        if (!urb || USB_GET_ENDPOINT_TYPE(urb->ep->bmAttributes) != USB_ENDPOINT_TYPE_CONTROL) {
+            continue;
+        }
+
+        hcchar = dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCCHAR));
+        hcint = dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCINT));
+
+        if ((hcchar & USB_OTG_HCCHAR_CHENA) == 0U) {
+            continue;
+        }
+        if (hcint != 0U) {
+            continue;
+        }
+        if (chan->sof_kicks >= 4U) {
+            continue;
+        }
+        if (chan->last_start_frame == frame) {
+            continue;
+        }
+
+        hcchar &= ~(USB_OTG_HCCHAR_CHDIS | USB_OTG_HCCHAR_ODDFRM);
+        hcchar |= (((uint32_t)dwc2_rd32(bus, DWC2_HOST_OFF(HFNUM)) & 0x01U) != 0U) ? USB_OTG_HCCHAR_ODDFRM : 0U;
+        hcchar |= USB_OTG_HCCHAR_CHENA;
+        dwc2_hc_wr32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCCHAR), hcchar);
+
+        chan->last_start_frame = frame;
+        chan->sof_kicks++;
+
+        USB_LOG_INFO("Bellatrix DWC2: sof kick ch=%u frame=%u kicks=%u hcchar=0x%08x hctsiz=0x%08x hcdma=0x%08x\r\n",
+                     (unsigned int)i,
+                     (unsigned int)frame,
+                     (unsigned int)chan->sof_kicks,
+                     (unsigned int)dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCCHAR)),
+                     (unsigned int)dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCTSIZ)),
+                     (unsigned int)dwc2_hc_rd32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCDMA)));
+    }
+}
+
 static inline void dwc2_chan_enable_csplit(struct usbh_bus *bus, uint8_t ch_num, bool enable)
 {
     if (enable) {
-        USB_OTG_HC((uint32_t)ch_num)->HCSPLT |= USB_OTG_HCSPLT_COMPLSPLT;
+        dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCSPLT),
+                     dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCSPLT)) | USB_OTG_HCSPLT_COMPLSPLT);
     } else {
-        USB_OTG_HC((uint32_t)ch_num)->HCSPLT &= ~USB_OTG_HCSPLT_COMPLSPLT;
+        dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCSPLT),
+                     dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCSPLT)) & ~USB_OTG_HCSPLT_COMPLSPLT);
     }
 }
 
 static void dwc2_halt(struct usbh_bus *bus, uint8_t ch_num)
 {
-    volatile uint32_t ChannelEna = (USB_OTG_HC(ch_num)->HCCHAR & USB_OTG_HCCHAR_CHENA) >> 31;
+    volatile uint32_t ChannelEna = (dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR)) & USB_OTG_HCCHAR_CHENA) >> 31;
     volatile uint32_t count = 0U;
     volatile uint32_t value;
 
-    if (((USB_OTG_GLB->GAHBCFG & USB_OTG_GAHBCFG_DMAEN) == USB_OTG_GAHBCFG_DMAEN) &&
+    if (((dwc2_rd32(bus, DWC2_GLB_OFF(GAHBCFG)) & USB_OTG_GAHBCFG_DMAEN) == USB_OTG_GAHBCFG_DMAEN) &&
         (ChannelEna == 0U)) {
         return;
     }
 
-    USB_OTG_HC(ch_num)->HCINTMSK = 0;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINTMSK), 0U);
 
-    value = USB_OTG_HC(ch_num)->HCCHAR;
+    value = dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR));
     value |= USB_OTG_HCCHAR_CHDIS;
     value |= USB_OTG_HCCHAR_CHENA;
-    USB_OTG_HC(ch_num)->HCCHAR = value;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR), value);
     do {
         if (++count > 200000U) {
             break;
         }
-    } while (USB_OTG_HC(ch_num)->HCCHAR & USB_OTG_HCCHAR_CHENA);
+    } while (dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR)) & USB_OTG_HCCHAR_CHENA);
 
-    USB_OTG_HC(ch_num)->HCINT = USB_OTG_HC(ch_num)->HCINT;
+    dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINT),
+                 dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINT)));
 }
 
 static int usbh_reset_port(struct usbh_bus *bus, const uint8_t port)
 {
     __IO uint32_t hprt0 = 0U;
+    (void)port;
     volatile uint32_t timeout = 0;
 
-    hprt0 = USB_OTG_HPRT;
+    hprt0 = dwc2_rd32(bus, DWC2_HOST_OFF(HPRT));
 
     hprt0 &= ~(USB_OTG_HPRT_PENA | USB_OTG_HPRT_PCDET |
                USB_OTG_HPRT_PENCHNG | USB_OTG_HPRT_POCCHNG);
 
-    USB_OTG_HPRT = (USB_OTG_HPRT_PRST | hprt0);
+    dwc2_wr32(bus, DWC2_HOST_OFF(HPRT), (USB_OTG_HPRT_PRST | hprt0));
     usb_osal_msleep(100U);
-    USB_OTG_HPRT = ((~USB_OTG_HPRT_PRST) & hprt0);
+    dwc2_wr32(bus, DWC2_HOST_OFF(HPRT), ((~USB_OTG_HPRT_PRST) & hprt0));
     usb_osal_msleep(10U);
 
-    while (!(USB_OTG_HPRT & USB_OTG_HPRT_PENA)) {
-        if (!(USB_OTG_HPRT & USB_OTG_HPRT_PCSTS)) {
+    while (!(dwc2_rd32(bus, DWC2_HOST_OFF(HPRT)) & USB_OTG_HPRT_PENA)) {
+        if (!(dwc2_rd32(bus, DWC2_HOST_OFF(HPRT)) & USB_OTG_HPRT_PCSTS)) {
             return -USB_ERR_NOTCONN; /* Port not connected */
         }
         timeout++;
@@ -409,8 +582,8 @@ static inline uint32_t dwc2_get_glb_intstatus(struct usbh_bus *bus)
 {
     uint32_t tmpreg;
 
-    tmpreg = USB_OTG_GLB->GINTSTS;
-    tmpreg &= USB_OTG_GLB->GINTMSK;
+    tmpreg = dwc2_rd32(bus, DWC2_GLB_OFF(GINTSTS));
+    tmpreg &= dwc2_rd32(bus, DWC2_GLB_OFF(GINTMSK));
 
     return tmpreg;
 }
@@ -437,8 +610,8 @@ uint32_t dwc2_calc_frame_interval(struct usbh_bus *bus)
     uint32_t hprt0;
     int clock = 60; /* default value */
 
-    usbcfg = USB_OTG_GLB->GUSBCFG;
-    hprt0 = USB_OTG_HPRT;
+    usbcfg = dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG));
+    hprt0 = dwc2_rd32(bus, DWC2_HOST_OFF(HPRT));
 
     if (!(usbcfg & USB_OTG_GUSBCFG_PHYSEL) && (usbcfg & USB_OTG_GUSBCFG_ULPI_UTMI_SEL) &&
         !(usbcfg & USB_OTG_GUSBCFG_PHYIF16))
@@ -554,6 +727,15 @@ static void dwc2_control_urb_init(struct usbh_bus *bus, uint8_t chidx, struct us
 
     if (chan->ep0_state == DWC2_EP0_STATE_SETUP) /* fill setup */
     {
+        USB_LOG_INFO("Bellatrix DWC2: setup bytes %02x %02x %02x %02x %02x %02x %02x %02x\r\n",
+                     (unsigned int)setup->bmRequestType,
+                     (unsigned int)setup->bRequest,
+                     (unsigned int)(setup->wValue & 0xffu),
+                     (unsigned int)((setup->wValue >> 8) & 0xffu),
+                     (unsigned int)(setup->wIndex & 0xffu),
+                     (unsigned int)((setup->wIndex >> 8) & 0xffu),
+                     (unsigned int)(setup->wLength & 0xffu),
+                     (unsigned int)((setup->wLength >> 8) & 0xffu));
         chan->num_packets = dwc2_calculate_packet_num(8, 0x00, USB_GET_MAXPACKETSIZE(urb->ep->wMaxPacketSize), &chan->xferlen);
         dwc2_chan_init(bus,
                        chidx,
@@ -676,12 +858,12 @@ int usb_hc_init(struct usbh_bus *bus)
     usb_hc_low_level_init(bus);
 
     USB_LOG_INFO("========== dwc2 hcd params ==========\r\n");
-    USB_LOG_INFO("CID:%08x\r\n", (unsigned int)USB_OTG_GLB->CID);
-    USB_LOG_INFO("GSNPSID:%08x\r\n", (unsigned int)USB_OTG_GLB->GSNPSID);
-    USB_LOG_INFO("GHWCFG1:%08x\r\n", (unsigned int)USB_OTG_GLB->GHWCFG1);
-    USB_LOG_INFO("GHWCFG2:%08x\r\n", (unsigned int)USB_OTG_GLB->GHWCFG2);
-    USB_LOG_INFO("GHWCFG3:%08x\r\n", (unsigned int)USB_OTG_GLB->GHWCFG3);
-    USB_LOG_INFO("GHWCFG4:%08x\r\n", (unsigned int)USB_OTG_GLB->GHWCFG4);
+    USB_LOG_INFO("CID:%08x\r\n", (unsigned int)dwc2_rd32(bus, DWC2_GLB_OFF(CID)));
+    USB_LOG_INFO("GSNPSID:%08x\r\n", (unsigned int)dwc2_rd32(bus, DWC2_GLB_OFF(GSNPSID)));
+    USB_LOG_INFO("GHWCFG1:%08x\r\n", (unsigned int)dwc2_rd32(bus, DWC2_GLB_OFF(GHWCFG1)));
+    USB_LOG_INFO("GHWCFG2:%08x\r\n", (unsigned int)dwc2_rd32(bus, DWC2_GLB_OFF(GHWCFG2)));
+    USB_LOG_INFO("GHWCFG3:%08x\r\n", (unsigned int)dwc2_rd32(bus, DWC2_GLB_OFF(GHWCFG3)));
+    USB_LOG_INFO("GHWCFG4:%08x\r\n", (unsigned int)dwc2_rd32(bus, DWC2_GLB_OFF(GHWCFG4)));
 
     dwc2_get_hwparams(bus->hcd.reg_base, &g_dwc2_hcd[bus->hcd.hcd_id].hw_params);
     dwc2_get_user_params(bus->hcd.reg_base, &g_dwc2_hcd[bus->hcd.hcd_id].user_params);
@@ -700,18 +882,26 @@ int usb_hc_init(struct usbh_bus *bus)
     USB_LOG_INFO("dwc2 has %d channels and dfifo depth(32-bit words) is %d\r\n",
                  g_dwc2_hcd[bus->hcd.hcd_id].hw_params.host_channels,
                  g_dwc2_hcd[bus->hcd.hcd_id].user_params.total_fifo_size);
+    USB_LOG_INFO("Bellatrix DWC2: user phy_type=%u utmi_width=%u gccfg=0x%08x fifo(rx=%u np=%u p=%u)\r\n",
+                 (unsigned int)g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_type,
+                 (unsigned int)g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_utmi_width,
+                 (unsigned int)g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_gccfg,
+                 (unsigned int)g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size,
+                 (unsigned int)g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size,
+                 (unsigned int)g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_perio_tx_fifo_size);
 
     USB_ASSERT_MSG(g_dwc2_hcd[bus->hcd.hcd_id].hw_params.arch == GHWCFG2_INT_DMA_ARCH, "This dwc2 version does not support dma mode, so stop working");
-    USB_ASSERT_MSG((g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size +
-                    g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size +
-                    g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_perio_tx_fifo_size) <=
+    USB_ASSERT_MSG(((uint32_t)g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size +
+                    (uint32_t)g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size +
+                    (uint32_t)g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_perio_tx_fifo_size) <=
                        g_dwc2_hcd[bus->hcd.hcd_id].user_params.total_fifo_size,
                    "Your fifo config is overflow, please check");
 
-    USB_OTG_GLB->GAHBCFG &= ~USB_OTG_GAHBCFG_GINT;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GAHBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GAHBCFG)) & ~USB_OTG_GAHBCFG_GINT);
 
     /* This is vendor register */
-    USB_OTG_GLB->GCCFG = g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_gccfg;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GCCFG), g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_gccfg);
 
     if (g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_type != DWC2_PHY_TYPE_PARAM_FS) {
         USB_ASSERT_MSG(g_dwc2_hcd[bus->hcd.hcd_id].hw_params.hs_phy_type != 0, "This dwc2 version does not support hs, so stop working");
@@ -722,79 +912,113 @@ int usb_hc_init(struct usbh_bus *bus)
     /* Force Host Mode*/
     dwc2_set_mode(bus, USB_OTG_MODE_HOST);
 
-    USB_ASSERT_MSG((USB_OTG_GLB->GRXFSIZ & 0xffff) >= g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size,
-                   "host_rx_fifo_size cannot be larger than power_on_value %u", (unsigned int)(USB_OTG_GLB->GRXFSIZ & 0xffff));
-    USB_ASSERT_MSG(((USB_OTG_GLB->DIEPTXF0_HNPTXFSIZ >> 16) & 0xffff) >= g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size,
-                   "host_nperio_tx_fifo_size cannot be larger than power_on_value %u", (unsigned int)((USB_OTG_GLB->DIEPTXF0_HNPTXFSIZ >> 16) & 0xffff));
-    USB_ASSERT_MSG(((USB_OTG_GLB->HPTXFSIZ >> 16) & 0xffff) >= g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_perio_tx_fifo_size,
-                   "host_perio_tx_fifo_size cannot be larger than power_on_value %u", (unsigned int)((USB_OTG_GLB->HPTXFSIZ >> 16) & 0xffff));
+    USB_ASSERT_MSG((dwc2_rd32(bus, DWC2_GLB_OFF(GRXFSIZ)) & 0xffff) >= g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size,
+                   "host_rx_fifo_size cannot be larger than power_on_value %u", (unsigned int)(dwc2_rd32(bus, DWC2_GLB_OFF(GRXFSIZ)) & 0xffff));
+    USB_ASSERT_MSG(((dwc2_rd32(bus, DWC2_GLB_OFF(DIEPTXF0_HNPTXFSIZ)) >> 16) & 0xffff) >= g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size,
+                   "host_nperio_tx_fifo_size cannot be larger than power_on_value %u", (unsigned int)((dwc2_rd32(bus, DWC2_GLB_OFF(DIEPTXF0_HNPTXFSIZ)) >> 16) & 0xffff));
+    USB_ASSERT_MSG(((dwc2_rd32(bus, DWC2_GLB_OFF(HPTXFSIZ)) >> 16) & 0xffff) >= g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_perio_tx_fifo_size,
+                   "host_perio_tx_fifo_size cannot be larger than power_on_value %u", (unsigned int)((dwc2_rd32(bus, DWC2_GLB_OFF(HPTXFSIZ)) >> 16) & 0xffff));
 
     /* B-peripheral session valid override enable */
-    USB_OTG_GLB->GOTGCTL &= ~USB_OTG_GOTGCTL_BVALOEN;
-    USB_OTG_GLB->GOTGCTL &= ~USB_OTG_GOTGCTL_BVALOVAL;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GOTGCTL),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GOTGCTL)) & ~USB_OTG_GOTGCTL_BVALOEN);
+    dwc2_wr32(bus, DWC2_GLB_OFF(GOTGCTL),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GOTGCTL)) & ~USB_OTG_GOTGCTL_BVALOVAL);
 
-    USB_OTG_GLB->GUSBCFG |= USB_OTG_GUSBCFG_TOCAL;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GUSBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG)) | USB_OTG_GUSBCFG_TOCAL);
 
     /* Restart the Phy Clock */
-    USB_OTG_PCGCCTL = 0U;
+    dwc2_wr32(bus, USB_OTG_PCGCCTL_BASE, 0U);
 
     /* Set default Max speed support */
-    USB_OTG_HOST->HCFG &= ~USB_OTG_HCFG_FSLSS;
+    dwc2_wr32(bus, DWC2_HOST_OFF(HCFG),
+              dwc2_rd32(bus, DWC2_HOST_OFF(HCFG)) & ~USB_OTG_HCFG_FSLSS);
 
-    USB_OTG_HOST->HCFG &= ~USB_OTG_HCFG_FSLSPCS;
+    dwc2_wr32(bus, DWC2_HOST_OFF(HCFG),
+              dwc2_rd32(bus, DWC2_HOST_OFF(HCFG)) & ~USB_OTG_HCFG_FSLSPCS);
     if (g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_type == DWC2_PHY_TYPE_PARAM_FS) {
         bus->hcd.roothub.speed = USB_SPEED_FULL;
-        USB_OTG_HOST->HCFG |= USB_OTG_HCFG_FSLSPCLKSEL_48_MHZ;
+        dwc2_wr32(bus, DWC2_HOST_OFF(HCFG),
+                  dwc2_rd32(bus, DWC2_HOST_OFF(HCFG)) | USB_OTG_HCFG_FSLSPCLKSEL_48_MHZ);
     } else {
         bus->hcd.roothub.speed = USB_SPEED_HIGH;
-        USB_OTG_HOST->HCFG |= USB_OTG_HCFG_FSLSPCLKSEL_30_60_MHZ;
+        dwc2_wr32(bus, DWC2_HOST_OFF(HCFG),
+                  dwc2_rd32(bus, DWC2_HOST_OFF(HCFG)) | USB_OTG_HCFG_FSLSPCLKSEL_30_60_MHZ);
     }
 
     if (g_dwc2_hcd[bus->hcd.hcd_id].hw_params.snpsid > 0x4F54292AU) {
-        USB_OTG_HOST->HFIR |= USB_OTG_HFIR_RELOAD_CTRL;
+        dwc2_wr32(bus, DWC2_HOST_OFF(HFIR),
+                  dwc2_rd32(bus, DWC2_HOST_OFF(HFIR)) | USB_OTG_HFIR_RELOAD_CTRL);
     }
 
     /* Clear all pending HC Interrupts */
     for (uint8_t i = 0U; i < g_dwc2_hcd[bus->hcd.hcd_id].hw_params.host_channels; i++) {
-        USB_OTG_HC(i)->HCINT = 0xFFFFFFFFU;
-        USB_OTG_HC(i)->HCINTMSK = 0U;
+        dwc2_hc_wr32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCINT), 0xFFFFFFFFU);
+        dwc2_hc_wr32(bus, i, offsetof(DWC2_HostChannelTypeDef, HCINTMSK), 0U);
     }
 
     /* Disable all interrupts. */
-    USB_OTG_GLB->GINTMSK = 0U;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GINTMSK), 0U);
 
     /* Clear any pending interrupts */
-    USB_OTG_GLB->GINTSTS = 0xFFFFFFFFU;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GINTSTS), 0xFFFFFFFFU);
 
     /* set FIFO size */
-    USB_OTG_GLB->GRXFSIZ = g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size;
-    USB_OTG_GLB->DIEPTXF0_HNPTXFSIZ = (uint32_t)(((g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size << 16) & USB_OTG_NPTXFD) |
-                                                 g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size);
-    USB_OTG_GLB->HPTXFSIZ = (uint32_t)(((g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_perio_tx_fifo_size << 16) & USB_OTG_HPTXFSIZ_PTXFD) |
-                                       (g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size + g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size));
+    dwc2_wr32(bus, DWC2_GLB_OFF(GRXFSIZ), g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size);
+    dwc2_wr32(bus, DWC2_GLB_OFF(DIEPTXF0_HNPTXFSIZ),
+              (uint32_t)(((g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size << 16) & USB_OTG_NPTXFD) |
+                         g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size));
+    dwc2_wr32(bus, DWC2_GLB_OFF(HPTXFSIZ),
+              (uint32_t)(((g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_perio_tx_fifo_size << 16) & USB_OTG_HPTXFSIZ_PTXFD) |
+                         (g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_rx_fifo_size + g_dwc2_hcd[bus->hcd.hcd_id].user_params.host_nperio_tx_fifo_size)));
 
     ret = dwc2_flush_txfifo(bus, 0x10U);
     ret = dwc2_flush_rxfifo(bus);
 
-    USB_OTG_GLB->GAHBCFG &= ~USB_OTG_GAHBCFG_HBSTLEN;
-    USB_OTG_GLB->GAHBCFG |= USB_OTG_GAHBCFG_HBSTLEN_4;
-    USB_OTG_GLB->GAHBCFG |= USB_OTG_GAHBCFG_DMAEN;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GAHBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GAHBCFG)) & ~USB_OTG_GAHBCFG_HBSTLEN);
+    dwc2_wr32(bus, DWC2_GLB_OFF(GAHBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GAHBCFG)) | USB_OTG_GAHBCFG_HBSTLEN_4);
+    dwc2_wr32(bus, DWC2_GLB_OFF(GAHBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GAHBCFG)) | USB_OTG_GAHBCFG_DMAEN);
 
     /* Enable interrupts matching to the Host mode ONLY */
-    USB_OTG_GLB->GINTMSK |= (USB_OTG_GINTMSK_PRTIM | USB_OTG_GINTMSK_HCIM |
-                             USB_OTG_GINTSTS_DISCINT);
+    dwc2_wr32(bus, DWC2_GLB_OFF(GINTMSK),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GINTMSK)) |
+                  (USB_OTG_GINTMSK_SOFM |
+                   USB_OTG_GINTMSK_PRTIM | USB_OTG_GINTMSK_HCIM |
+                   USB_OTG_GINTSTS_DISCINT));
 
     dwc2_drivebus(bus, 1);
     usb_osal_msleep(200);
 
-    USB_OTG_GLB->GAHBCFG |= USB_OTG_GAHBCFG_GINT;
+    USB_LOG_INFO("Bellatrix DWC2: post-core GUSBCFG=0x%08x HCFG=0x%08x HPRT=0x%08x\r\n",
+                 (unsigned int)dwc2_rd32(bus, DWC2_GLB_OFF(GUSBCFG)),
+                 (unsigned int)dwc2_rd32(bus, DWC2_HOST_OFF(HCFG)),
+                 (unsigned int)dwc2_rd32(bus, DWC2_HOST_OFF(HPRT)));
+
+    if (dwc2_rd32(bus, DWC2_HOST_OFF(HPRT)) & USB_OTG_HPRT_PCSTS) {
+        g_dwc2_hcd[bus->hcd.hcd_id].port_csc = 1;
+        bus->hcd.roothub.int_buffer[0] = (1 << 1);
+        bus->hcd.roothub.int_buffer[1] = 0;
+        USB_LOG_INFO("Bellatrix DWC2: seeded root-hub wakeup hprt=0x%08x csc=%u intbuf=0x%02x\r\n",
+                     (unsigned int)dwc2_rd32(bus, DWC2_HOST_OFF(HPRT)),
+                     (unsigned int)g_dwc2_hcd[bus->hcd.hcd_id].port_csc,
+                     (unsigned int)bus->hcd.roothub.int_buffer[0]);
+        usbh_hub_thread_wakeup(&bus->hcd.roothub);
+    }
+
+    dwc2_wr32(bus, DWC2_GLB_OFF(GAHBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GAHBCFG)) | USB_OTG_GAHBCFG_GINT);
 
     return ret;
 }
 
 int usb_hc_deinit(struct usbh_bus *bus)
 {
-    USB_OTG_GLB->GAHBCFG &= ~USB_OTG_GAHBCFG_GINT;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GAHBCFG),
+              dwc2_rd32(bus, DWC2_GLB_OFF(GAHBCFG)) & ~USB_OTG_GAHBCFG_GINT);
 
     dwc2_flush_txfifo(bus, 0x10U);
     dwc2_flush_rxfifo(bus);
@@ -805,11 +1029,11 @@ int usb_hc_deinit(struct usbh_bus *bus)
     }
 
     /* Disable all interrupts. */
-    USB_OTG_GLB->GINTMSK = 0U;
+    dwc2_wr32(bus, DWC2_GLB_OFF(GINTMSK), 0U);
 
     /* Clear any pending Host interrupts */
-    USB_OTG_HOST->HAINT = 0xFFFFFFFFU;
-    USB_OTG_GLB->GINTSTS = 0xFFFFFFFFU;
+    dwc2_wr32(bus, DWC2_HOST_OFF(HAINT), 0xFFFFFFFFU);
+    dwc2_wr32(bus, DWC2_GLB_OFF(GINTSTS), 0xFFFFFFFFU);
 
     dwc2_drivebus(bus, 0);
     usb_osal_msleep(200);
@@ -824,7 +1048,7 @@ int usb_hc_deinit(struct usbh_bus *bus)
 
 uint16_t usbh_get_frame_number(struct usbh_bus *bus)
 {
-    return (USB_OTG_HOST->HFNUM & USB_OTG_HFNUM_FRNUM);
+    return (dwc2_rd32(bus, DWC2_HOST_OFF(HFNUM)) & USB_OTG_HFNUM_FRNUM);
 }
 
 int usbh_roothub_control(struct usbh_bus *bus, struct usb_setup_packet *setup, uint8_t *buf)
@@ -875,7 +1099,8 @@ int usbh_roothub_control(struct usbh_bus *bus, struct usb_setup_packet *setup, u
 
                 switch (setup->wValue) {
                     case HUB_PORT_FEATURE_ENABLE:
-                        USB_OTG_HPRT &= ~USB_OTG_HPRT_PENA;
+                        dwc2_wr32(bus, DWC2_HOST_OFF(HPRT),
+                                  dwc2_rd32(bus, DWC2_HOST_OFF(HPRT)) & ~USB_OTG_HPRT_PENA);
                         break;
                     case HUB_PORT_FEATURE_SUSPEND:
                     case HUB_PORT_FEATURE_C_SUSPEND:
@@ -920,7 +1145,7 @@ int usbh_roothub_control(struct usbh_bus *bus, struct usb_setup_packet *setup, u
                 if (!port || port > nports) {
                     return -USB_ERR_INVAL;
                 }
-                hprt0 = USB_OTG_HPRT;
+                hprt0 = dwc2_rd32(bus, DWC2_HOST_OFF(HPRT));
 
                 status = 0;
                 if (g_dwc2_hcd[bus->hcd.hcd_id].port_csc) {
@@ -954,7 +1179,12 @@ int usbh_roothub_control(struct usbh_bus *bus, struct usb_setup_packet *setup, u
                     status |= (1 << HUB_PORT_FEATURE_POWER);
                 }
 
-                memcpy(buf, &status, 4);
+                if (buf) {
+                    buf[0] = (uint8_t)(status & 0xffU);
+                    buf[1] = (uint8_t)((status >> 8) & 0xffU);
+                    buf[2] = (uint8_t)((status >> 16) & 0xffU);
+                    buf[3] = (uint8_t)((status >> 24) & 0xffU);
+                }
                 break;
             default:
                 break;
@@ -986,7 +1216,7 @@ int usbh_submit_urb(struct usbh_urb *urb)
 #endif
     bus = urb->hport->bus;
 
-    if (!(USB_OTG_HPRT & USB_OTG_HPRT_PCSTS) || !urb->hport->connected) {
+    if (!(dwc2_rd32(bus, DWC2_HOST_OFF(HPRT)) & USB_OTG_HPRT_PCSTS) || !urb->hport->connected) {
         return -USB_ERR_NOTCONN;
     }
 
@@ -1153,17 +1383,29 @@ static void dwc2_inchan_irq_handler(struct usbh_bus *bus, uint8_t ch_num)
     struct dwc2_chan *chan;
     struct usbh_urb *urb;
 
-    chan_intstatus = USB_OTG_HC(ch_num)->HCINT;
+    chan_intstatus = dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINT));
 
     chan = &g_dwc2_hcd[bus->hcd.hcd_id].chan_pool[ch_num];
     urb = chan->urb;
+    if (urb && USB_GET_ENDPOINT_TYPE(urb->ep->bmAttributes) == USB_ENDPOINT_TYPE_CONTROL) {
+        USB_LOG_INFO("Bellatrix DWC2: in irq ch=%u hcint=0x%08x state=%u hctsiz=0x%08x hcchar=0x%08x err=%d actual=%u remain=%u\r\n",
+                     (unsigned int)ch_num,
+                     (unsigned int)chan_intstatus,
+                     (unsigned int)chan->ep0_state,
+                     (unsigned int)dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCTSIZ)),
+                     (unsigned int)dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR)),
+                     urb->errorcode,
+                     (unsigned int)urb->actual_length,
+                     (unsigned int)urb->transfer_buffer_length);
+    }
     //printf("s1:%08x\r\n", chan_intstatus);
 
     if (chan_intstatus & USB_OTG_HCINT_CHH) {
-        USB_OTG_HC(ch_num)->HCINT = chan_intstatus;
+        dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINT), chan_intstatus);
         if (chan_intstatus & USB_OTG_HCINT_XFRC) {
-            uint32_t count = chan->xferlen - (USB_OTG_HC(ch_num)->HCTSIZ & USB_OTG_HCTSIZ_XFRSIZ); /* how many size has received */
-            uint8_t data_toggle = ((USB_OTG_HC(ch_num)->HCTSIZ & USB_OTG_HCTSIZ_DPID) >> USB_OTG_HCTSIZ_DPID_Pos);
+            uint32_t hctsiz = dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCTSIZ));
+            uint32_t count = chan->xferlen - (hctsiz & USB_OTG_HCTSIZ_XFRSIZ); /* how many size has received */
+            uint8_t data_toggle = ((hctsiz & USB_OTG_HCTSIZ_DPID) >> USB_OTG_HCTSIZ_DPID_Pos);
 
             urb->actual_length += count;
             urb->transfer_buffer_length -= count;
@@ -1295,19 +1537,31 @@ static void dwc2_outchan_irq_handler(struct usbh_bus *bus, uint8_t ch_num)
     struct dwc2_chan *chan;
     struct usbh_urb *urb;
 
-    chan_intstatus = USB_OTG_HC(ch_num)->HCINT;
+    chan_intstatus = dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINT));
 
     chan = &g_dwc2_hcd[bus->hcd.hcd_id].chan_pool[ch_num];
     urb = chan->urb;
+    if (urb && USB_GET_ENDPOINT_TYPE(urb->ep->bmAttributes) == USB_ENDPOINT_TYPE_CONTROL) {
+        USB_LOG_INFO("Bellatrix DWC2: out irq ch=%u hcint=0x%08x state=%u hctsiz=0x%08x hcchar=0x%08x err=%d actual=%u remain=%u\r\n",
+                     (unsigned int)ch_num,
+                     (unsigned int)chan_intstatus,
+                     (unsigned int)chan->ep0_state,
+                     (unsigned int)dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCTSIZ)),
+                     (unsigned int)dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCCHAR)),
+                     urb->errorcode,
+                     (unsigned int)urb->actual_length,
+                     (unsigned int)urb->transfer_buffer_length);
+    }
     //printf("s2:%08x\r\n", chan_intstatus);
 
     if (chan_intstatus & USB_OTG_HCINT_CHH) {
-        USB_OTG_HC(ch_num)->HCINT = chan_intstatus;
+        dwc2_hc_wr32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCINT), chan_intstatus);
         if (chan_intstatus & USB_OTG_HCINT_XFRC) {
-            uint32_t count = USB_OTG_HC(ch_num)->HCTSIZ & USB_OTG_HCTSIZ_XFRSIZ;                                          /* last packet size */
-            uint32_t has_used_packets = chan->num_packets - ((USB_OTG_HC(ch_num)->HCTSIZ & USB_OTG_HCTSIZ_PKTCNT) >> 19); /* how many packets have used */
+            uint32_t hctsiz = dwc2_hc_rd32(bus, ch_num, offsetof(DWC2_HostChannelTypeDef, HCTSIZ));
+            uint32_t count = hctsiz & USB_OTG_HCTSIZ_XFRSIZ;                                          /* last packet size */
+            uint32_t has_used_packets = chan->num_packets - ((hctsiz & USB_OTG_HCTSIZ_PKTCNT) >> 19); /* how many packets have used */
             uint32_t olen = (has_used_packets - 1) * USB_GET_MAXPACKETSIZE(urb->ep->wMaxPacketSize) + count;              /* the same with urb->actual_length += chan->xferlen; */
-            uint8_t data_toggle = ((USB_OTG_HC(ch_num)->HCTSIZ & USB_OTG_HCTSIZ_DPID) >> USB_OTG_HCTSIZ_DPID_Pos);
+            uint8_t data_toggle = ((hctsiz & USB_OTG_HCTSIZ_DPID) >> USB_OTG_HCTSIZ_DPID_Pos);
 
             urb->actual_length += olen;
 
@@ -1456,8 +1710,9 @@ static void dwc2_port_irq_handler(struct usbh_bus *bus)
     __IO uint32_t hprt0, hprt0_dup, regval;
 
     /* Handle Host Port Interrupts */
-    hprt0 = USB_OTG_HPRT;
-    hprt0_dup = USB_OTG_HPRT;
+    hprt0 = dwc2_rd32(bus, DWC2_HOST_OFF(HPRT));
+    hprt0_dup = hprt0;
+    USB_LOG_INFO("Bellatrix DWC2: HPRT irq raw=0x%08x\r\n", (unsigned int)hprt0);
 
     hprt0_dup &= ~(USB_OTG_HPRT_PENA | USB_OTG_HPRT_PCDET |
                    USB_OTG_HPRT_PENCHNG | USB_OTG_HPRT_POCCHNG);
@@ -1466,7 +1721,14 @@ static void dwc2_port_irq_handler(struct usbh_bus *bus)
     if ((hprt0 & USB_OTG_HPRT_PCDET) == USB_OTG_HPRT_PCDET) {
         if ((hprt0 & USB_OTG_HPRT_PCSTS) == USB_OTG_HPRT_PCSTS) {
             bus->hcd.roothub.int_buffer[0] = (1 << 1);
+            bus->hcd.roothub.int_buffer[1] = 0;
             usbh_hub_thread_wakeup(&bus->hcd.roothub);
+            USB_LOG_INFO("Bellatrix DWC2: connect detected hprt=0x%08x intbuf=0x%02x\r\n",
+                         (unsigned int)hprt0,
+                         (unsigned int)bus->hcd.roothub.int_buffer[0]);
+        } else {
+            USB_LOG_INFO("Bellatrix DWC2: disconnect detected hprt=0x%08x\r\n",
+                         (unsigned int)hprt0);
         }
         hprt0_dup |= USB_OTG_HPRT_PCDET;
         g_dwc2_hcd[bus->hcd.hcd_id].port_csc = 1;
@@ -1478,28 +1740,25 @@ static void dwc2_port_irq_handler(struct usbh_bus *bus)
         g_dwc2_hcd[bus->hcd.hcd_id].port_pec = 1;
 
         if ((hprt0 & USB_OTG_HPRT_PENA) == USB_OTG_HPRT_PENA) {
-            regval = USB_OTG_HOST->HFIR;
+            regval = dwc2_rd32(bus, DWC2_HOST_OFF(HFIR));
             regval &= ~USB_OTG_HFIR_FRIVL;
             regval |= dwc2_calc_frame_interval(bus) & USB_OTG_HFIR_FRIVL;
-            USB_OTG_HOST->HFIR = regval;
-
-            if (g_dwc2_hcd[bus->hcd.hcd_id].user_params.phy_type == DWC2_PHY_TYPE_PARAM_FS) {
-                if ((hprt0 & USB_OTG_HPRT_PSPD) == (HPRT0_PRTSPD_LOW_SPEED << 17)) {
-                    if ((USB_OTG_HOST->HCFG & USB_OTG_HCFG_FSLSPCS) != USB_OTG_HCFG_FSLSPCLKSEL_6_MHZ) {
-                        regval = USB_OTG_HOST->HCFG;
-                        regval &= ~USB_OTG_HCFG_FSLSPCS;
-                        regval |= USB_OTG_HCFG_FSLSPCLKSEL_6_MHZ;
-                        USB_OTG_HOST->HCFG = regval;
-                    }
-                } else {
-                    if ((USB_OTG_HOST->HCFG & USB_OTG_HCFG_FSLSPCS) != USB_OTG_HCFG_FSLSPCLKSEL_48_MHZ) {
-                        regval = USB_OTG_HOST->HCFG;
-                        regval &= ~USB_OTG_HCFG_FSLSPCS;
-                        regval |= USB_OTG_HCFG_FSLSPCLKSEL_48_MHZ;
-                        USB_OTG_HOST->HCFG = regval;
-                    }
-                }
+            dwc2_wr32(bus, DWC2_HOST_OFF(HFIR), regval);
+
+            regval = dwc2_rd32(bus, DWC2_HOST_OFF(HCFG));
+            regval &= ~(USB_OTG_HCFG_FSLSPCS | USB_OTG_HCFG_FSLSS);
+            if ((hprt0 & USB_OTG_HPRT_PSPD) == (HPRT0_PRTSPD_LOW_SPEED << 17)) {
+                regval |= USB_OTG_HCFG_FSLSPCLKSEL_6_MHZ | USB_OTG_HCFG_FSLSS;
+            } else if ((hprt0 & USB_OTG_HPRT_PSPD) == (HPRT0_PRTSPD_FULL_SPEED << 17)) {
+                regval |= USB_OTG_HCFG_FSLSPCLKSEL_48_MHZ | USB_OTG_HCFG_FSLSS;
+            } else {
+                regval |= USB_OTG_HCFG_FSLSPCLKSEL_30_60_MHZ;
             }
+            dwc2_wr32(bus, DWC2_HOST_OFF(HCFG), regval);
+            USB_LOG_INFO("Bellatrix DWC2: port enable speed=%u hcfg=0x%08x hfir=0x%08x\r\n",
+                         (unsigned int)((hprt0 & USB_OTG_HPRT_PSPD) >> USB_OTG_HPRT_PSPD_Pos),
+                         (unsigned int)dwc2_rd32(bus, DWC2_HOST_OFF(HCFG)),
+                         (unsigned int)dwc2_rd32(bus, DWC2_HOST_OFF(HFIR)));
         } else {
         }
     }
@@ -1510,44 +1769,42 @@ static void dwc2_port_irq_handler(struct usbh_bus *bus)
         g_dwc2_hcd[bus->hcd.hcd_id].port_occ = 1;
     }
     /* Clear Port Interrupts */
-    USB_OTG_HPRT = hprt0_dup;
+    dwc2_wr32(bus, DWC2_HOST_OFF(HPRT), hprt0_dup);
 }
 
 void USBH_IRQHandler(uint8_t busid)
 {
-    uint32_t gint_status, chan_int;
+    uint32_t gint_status;
     struct usbh_bus *bus;
 
     bus = &g_usbhost_bus[busid];
     gint_status = dwc2_get_glb_intstatus(bus);
-    if ((USB_OTG_GLB->GINTSTS & 0x1U) == USB_OTG_MODE_HOST) {
+    if ((dwc2_rd32(bus, DWC2_GLB_OFF(GINTSTS)) & 0x1U) == USB_OTG_MODE_HOST) {
         /* Avoid spurious interrupt */
         if (gint_status == 0) {
+            if (dwc2_service_pending_channels(bus, true)) {
+                return;
+            }
             return;
         }
 
         if (gint_status & USB_OTG_GINTSTS_HPRTINT) {
             dwc2_port_irq_handler(bus);
         }
+        if (gint_status & USB_OTG_GINTSTS_SOF) {
+            dwc2_sof_kick_pending_channels(bus);
+            dwc2_wr32(bus, DWC2_GLB_OFF(GINTSTS), USB_OTG_GINTSTS_SOF);
+        }
         if (gint_status & USB_OTG_GINTSTS_DISCINT) {
             g_dwc2_hcd[bus->hcd.hcd_id].port_csc = 1;
             bus->hcd.roothub.int_buffer[0] = (1 << 1);
             usbh_hub_thread_wakeup(&bus->hcd.roothub);
 
-            USB_OTG_GLB->GINTSTS = USB_OTG_GINTSTS_DISCINT;
+            dwc2_wr32(bus, DWC2_GLB_OFF(GINTSTS), USB_OTG_GINTSTS_DISCINT);
         }
         if (gint_status & USB_OTG_GINTSTS_HCINT) {
-            chan_int = (USB_OTG_HOST->HAINT & USB_OTG_HOST->HAINTMSK) & 0xFFFFU;
-            for (uint8_t i = 0U; i < g_dwc2_hcd[bus->hcd.hcd_id].hw_params.host_channels; i++) {
-                if ((chan_int & (1UL << (i & 0xFU))) != 0U) {
-                    if ((USB_OTG_HC(i)->HCCHAR & USB_OTG_HCCHAR_EPDIR) == USB_OTG_HCCHAR_EPDIR) {
-                        dwc2_inchan_irq_handler(bus, i);
-                    } else {
-                        dwc2_outchan_irq_handler(bus, i);
-                    }
-                }
-            }
-            USB_OTG_GLB->GINTSTS = USB_OTG_GINTSTS_HCINT;
+            dwc2_service_pending_channels(bus, false);
+            dwc2_wr32(bus, DWC2_GLB_OFF(GINTSTS), USB_OTG_GINTSTS_HCINT);
         }
     }
-}
\ No newline at end of file
+}
