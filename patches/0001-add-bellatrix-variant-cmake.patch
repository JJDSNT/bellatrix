diff --git a/CMakeLists.txt b/CMakeLists.txt
index eb89404..127844d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -15,7 +15,7 @@ set(SUPPORTED_TARGETS "raspi64" "pbpro" "rockpro64" "virt")
 set(TARGET "raspi64" CACHE STRING "One of target machines: ${SUPPORTED_TARGETS}")
 set_property(CACHE TARGET PROPERTY STRINGS ${SUPPORTED_TARGETS})
 
-set(SUPPORTED_VARIANTS "none" "pistorm" "pistorm32lite")
+set(SUPPORTED_VARIANTS "none" "pistorm" "pistorm32lite" "bellatrix")
 set(VARIANT "none" CACHE STRING "Compilation variant: ${SUPPORTED_VARIANTS}")
 set_property(CACHE VARIANT PROPERTY STRINGS ${SUPPORTED_VARIANTS})
 
@@ -167,8 +167,41 @@ if (${VARIANT} IN_LIST SUPPORTED_VARIANTS)
         else()
             message(FATAL_ERROR "PiStorm variants are supported on raspi targets, only.")
         endif()
+    elseif(${VARIANT} STREQUAL "bellatrix")
+        if(${TARGET} STREQUAL "raspi64")
+            add_compile_definitions(BELLATRIX)
+            include_directories(${CMAKE_SOURCE_DIR}/../src)
+            include_directories(${CMAKE_SOURCE_DIR}/../src/cpu)
+            include_directories(${CMAKE_SOURCE_DIR}/../src/host)
+            list(APPEND BASE_FILES
+                ${CMAKE_SOURCE_DIR}/../src/cpu/bellatrix.c
+                ${CMAKE_SOURCE_DIR}/../src/core/machine.c
+                ${CMAKE_SOURCE_DIR}/../src/core/btrace.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/cia/cia.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/cia/cia_a.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/cia/cia_b.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/cia/cia_tod.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/rtc/rtc.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/agnus/agnus.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/agnus/blitter.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/agnus/copper.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/denise/denise.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/paula/paula.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/agnus/dma.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/agnus/beam.c
+                ${CMAKE_SOURCE_DIR}/../src/chipset/agnus/bitplanes.c
+                ${CMAKE_SOURCE_DIR}/../src/memory/memory.c
+                ${CMAKE_SOURCE_DIR}/../src/host/raspi3/pal_debug.c
+                ${CMAKE_SOURCE_DIR}/../src/host/raspi3/pal_ipl.c
+                ${CMAKE_SOURCE_DIR}/../src/host/raspi3/pal_core.c
+                ${CMAKE_SOURCE_DIR}/../src/host/raspi3/time.c
+                ${CMAKE_SOURCE_DIR}/../src/host/raspi3/posix_time.c
+            )
+        else()
+            message(FATAL_ERROR "Bellatrix variant is supported on raspi64 target only.")
+        endif()
     endif()
-    
+
 else()
     message(FATAL_ERROR "Wrong variant selected: ${VARIANT}")
 endif()
