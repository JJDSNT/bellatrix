--- a/m68k_in.c	2026-07-03 18:55:50.434807142 -0300
+++ b/m68k_in.c	2026-07-03 18:40:30.710324989 -0300
@@ -917,16 +917,18 @@
 
 M68KMAKE_OP(040fpu0, 32, ., .)
 {
-	if(CPU_TYPE_IS_030_PLUS(CPU_TYPE))
+	/* Bellatrix: dispatch for CPU_TYPE 68030 or 68040 (either macro
+	 * true — CPU_TYPE_IS_040_PLUS only needs M68K_EMULATE_040 on,
+	 * which matters for configs like the bare-metal build that enable
+	 * 040 without 030). 68000/010/020 fall through by design in this
+	 * Musashi variant, not a bug — see ISSUE-0034. */
+	if(CPU_TYPE_IS_040_PLUS(CPU_TYPE) || CPU_TYPE_IS_030_PLUS(CPU_TYPE))
 	{
 		m68040_fpu_op0();
 		return;
 	}
-	/* Bellatrix: only CPU_TYPE 68040 dispatches F2xx/F3xx to the FPU
-	 * emulation (see CPU_TYPE_IS_030_PLUS below, despite the name —
-	 * 020/030 fall through here by design in this Musashi variant, not
-	 * a bug). Logged so a real "68040 configured but still traps" bug
-	 * is distinguishable from the expected 020/030 case. See ISSUE-0031. */
+	/* Logged so a real "FPU-capable CPU still traps" bug is
+	 * distinguishable from the expected 000/010/020 case. */
 	{
 		static int s_hits = 0;
 		if (s_hits < 10) {
@@ -941,7 +943,7 @@
 
 M68KMAKE_OP(040fpu1, 32, ., .)
 {
-	if(CPU_TYPE_IS_030_PLUS(CPU_TYPE))
+	if(CPU_TYPE_IS_040_PLUS(CPU_TYPE) || CPU_TYPE_IS_030_PLUS(CPU_TYPE))
 	{
 		m68040_fpu_op1();
 		return;
